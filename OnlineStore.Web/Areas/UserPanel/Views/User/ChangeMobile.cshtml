@using OnlineStore.Domain.ViewModel.UserPanel
@model ChangeMobileViewModel
@{
    ViewData["Title"] = "ویرایش  موبایل";
}
<div class="ui-box bg-white mb-5">
    <div class="ui-box-title">@ViewData["Title"]</div>
    <form asp-area="UserPanel" asp-controller="User" asp-action="ChangeMobile" method="post">
        <input type="hidden" asp-for="UserId" />
        <div class="ui-box-content">
            <div class="row">
                <div class="col-lg-6 mb-3">
                    <input asp-for="Mobile" type="text" class="form-control" placeholder="موبایل" />
                </div>
                <div class="col-12 text-end">
                    <button type="submit" class="btn btn-primary px-4">ثبت و ارسال</button>
                </div>
                <div asp-validation-summary="All"></div>
            </div>
        </div>
    </form>
</div>
